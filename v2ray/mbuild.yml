---
REPO: v2ray
IMAGE_VERSION: v4.23.1

ARCH:
- name: arm64
  images:
    base: alpine:latest
    build_base: caddy:alpine
  tag: arm64
  arch: arm64
  platform: linux/arm64
  is_arm: true
  # enable: false

- name: amd64
  images:
    base: alpine:latest
    build_base: caddy:alpine
  tag: amd64
  arch: amd64
  platform: linux/amd64
  is_amd: true
  # enable: false

- name: armv7
  images:
    base: alpine:latest
    build_base: caddy:alpine
  tag: armv7
  arch: arm
  variant: v7
  platform: linux/arm/v7
  is_arm: true
  # enable: false

- name: armv6l
  images:
    base: alpine:latest
    build_base: caddy:alpine
  tag: armv6l
  arch: arm
  variant: v6l
  platform: linux/arm/v6
  is_arm: true
  # enable: false

- name: armv6
  images:
    base: alpine:latest
    build_base: caddy:alpine
  tag: armv6
  arch: arm
  variant: v6
  platform: linux/arm/v6
  is_arm: true
  # enable: false

- name: armv7l
  images:
    base: alpine:latest
    build_base: caddy:alpine
  tag: armv7l
  arch: arm
  variant: v7l
  platform: linux/arm/v7
  is_arm: true
  # enable: false

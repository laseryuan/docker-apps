on:
  workflow_dispatch:
  push:
    paths:
      - 'clasp/**'
      - '.github/workflows/clasp.yml'

jobs:
  build-push:
    uses: ./.github/workflows/build-push.yml
    secrets: inherit
    with:
      project-folder: clasp

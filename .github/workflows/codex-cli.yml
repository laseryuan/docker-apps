on:
  workflow_dispatch:
  push:
    paths:
      - 'codex-cli/**'
      - '.github/workflows/codex-cli.yml'

jobs:
  build-push:
    uses: ./.github/workflows/build-push.yml
    secrets: inherit
    with:
      project-folder: codex-cli
